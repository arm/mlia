## 1. Contract

- [x] 1.1 Define `inference_time` as the standard MLIA inference latency metric.
- [x] 1.2 Define `ms` as the standard unit for `inference_time`.
- [x] 1.3 Add `inference_time` to the standard performance metric set.
- [x] 1.4 Add unit validation for supplied standard performance metrics.

## 2. Documentation

- [x] 2.1 Document `inference_time` in the standardized metric availability list.

## 3. Validation

- [x] 3.1 Add test coverage for missing `inference_time` availability output.
- [x] 3.2 Add test coverage for rejecting a standard metric with a mismatched unit.
- [x] 3.3 Run targeted core tests for standardized output schema behavior.
- [x] 3.4 Validate the OpenSpec change.
